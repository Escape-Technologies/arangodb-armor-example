import type { Plugin } from '@envelop/core';
import { GraphQLArmorConfig } from '../config';
export declare const EnvelopArmorPlugin: (config?: GraphQLArmorConfig) => Plugin;
export declare class EnvelopArmor {
    private readonly protections;
    constructor(config?: GraphQLArmorConfig);
    protect(): {
        plugins: Plugin[];
    };
}
