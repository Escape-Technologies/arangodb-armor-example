import type { PluginDefinition, ValidationRule } from 'apollo-server-core';
import { GraphQLArmorConfig } from '../config';
export declare class ApolloArmor {
    private readonly protections;
    constructor(config?: GraphQLArmorConfig);
    protect(): {
        plugins: PluginDefinition[];
        validationRules: ValidationRule[];
        allowBatchedHttpRequests: false;
        debug: false;
    };
}
