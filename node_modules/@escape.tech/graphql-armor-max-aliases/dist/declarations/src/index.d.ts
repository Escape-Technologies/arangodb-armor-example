import type { Plugin } from '@envelop/core';
import { OperationDefinitionNode, ValidationContext } from 'graphql';
declare type MaxAliasesOptions = {
    n?: number;
};
declare class MaxAliasesVisitor {
    readonly OperationDefinition: Record<string, any>;
    private readonly context;
    private readonly config;
    private onError;
    constructor(context: ValidationContext, onError: (msg: string) => any, options?: MaxAliasesOptions);
    onOperationDefinitionEnter(operation: OperationDefinitionNode): void;
    private countAliases;
}
declare const maxAliasesRule: (onError: (msg: string) => any, options?: MaxAliasesOptions) => (context: ValidationContext) => MaxAliasesVisitor;
declare const maxAliasesPlugin: (options?: MaxAliasesOptions) => Plugin;
export { maxAliasesRule, maxAliasesPlugin, MaxAliasesOptions };
