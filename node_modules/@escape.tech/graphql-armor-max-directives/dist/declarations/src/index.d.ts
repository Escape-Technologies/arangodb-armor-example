import type { Plugin } from '@envelop/core';
import { OperationDefinitionNode, ValidationContext } from 'graphql';
declare type MaxDirectivesOptions = {
    n?: number;
};
declare class MaxDirectivesVisitor {
    readonly OperationDefinition: Record<string, any>;
    private readonly context;
    private readonly config;
    private readonly onError;
    constructor(context: ValidationContext, onError: (msg: string) => any, options?: MaxDirectivesOptions);
    onOperationDefinitionEnter(operation: OperationDefinitionNode): void;
    private countDirectives;
}
declare const maxDirectivesRule: (onError: (msg: string) => any, options?: MaxDirectivesOptions) => (context: ValidationContext) => MaxDirectivesVisitor;
declare const maxDirectivesPlugin: (options?: MaxDirectivesOptions) => Plugin;
export { maxDirectivesRule, MaxDirectivesOptions, maxDirectivesPlugin };
